一、首先介绍Windows下的使用

也可以直接找快捷方式去启动


安装的目录(tool所在目录)
D:\software\dmdbms\tool





二、其次介绍linux下的使用

找到DTS所在目录（服务器端）
比如DM8默认安装的目录，具体是tool还是tools我记不清了，容易记混。
有没有dmdba这一级目录，我也有点记住不住了。最终是视实际安装目录定，找到tool目录，里面有很多启动工具的脚本命令。
直接在终端运行（普通用户需要sudo提权，root用户直接运行）
/opt/dmdbms/bin/tool/DTS（这是我个人的安装路径）


直接在终端运行（普通用户需要sudo提权，root用户直接运行）
/opt/dmdbms/bin/tool/manager
